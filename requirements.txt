# WASEngine is installed in editable mode, no need to list it here unless used differently.
lupa
gdown
# Optional: Add a GUI framework, depending on your choice
# tkinter (comes with Python, no pip install needed)
# pygame (if you want canvas-based GUI)
# pyqt5 (if going for advanced GUI)